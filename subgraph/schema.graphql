enum LotteryStatus {
  OPEN
  STAKING
  CLOSED
  CANCELLED
}

type Lottery @entity {
  id: ID!
  address: Bytes!
  creator: Bytes!
  status: LotteryStatus!
  nftAddress: Bytes
  nftIndex: BigInt
  ticketPrice: BigInt
  created: BigInt
}
