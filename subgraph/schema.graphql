enum LotteryStatus {
  OPEN
  STAKING
  CLOSED
  CANCELLED
}

enum LotteryPoolType {
  STANDARD
  YIELD
}

type LotteryPool @entity {
  id: ID!
  address: Bytes!
  creator: Bytes!
  status: LotteryStatus!
  lotteryPoolType: LotteryPoolType!
  nftAddress: Bytes
  nftIndex: BigInt
  ticketPrice: BigInt
  minAmount: BigInt
  created: BigInt
  winner: Bytes
  finalPrice: BigInt
  fees: BigInt
}
